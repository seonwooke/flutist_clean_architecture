name: cart_data
description: A Flutter module
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  cart_domain:
    path: ../cart_domain

dev_dependencies:
  test: ^1.24.0

resolution: workspace