name: product_data
description: A Flutter module
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  dio: ^5.4.0
  product_domain:
    path: ../product_domain

dev_dependencies:
  test: ^1.24.0

resolution: workspace